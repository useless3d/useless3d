#pragma once

/*
#include <vector>
#include <memory>
#include <string>
#include "Rotation.h"
#include <optional>


namespace usls
{
    class Actor
    {
    private:
        //std::optional<Actor*>   parent; // we actually might not care about this
        std::string             name;
        std::string             space = "world"; // "world" or "view" determines if view matrix is set to identity or not
        


    public:
        std::string getName();
        void        setSpace(std::string space);


    };
}
*/